{
  "name": "learning-ledger-server",
  "version": "0.0.1",
  "description": "Learning Ledger server api",
  "main": "dist/server.js",
  "author": "Alex Persinger",
  "license": "MIT",
  "scripts": {
    "build-ts": "tsc",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "start-local": "set NODE_ENV=dev&& set DOCKER_HOST_IP=127.0.0.1&& node .",
    "start-dev": "export NODE_ENV=dev && export DOCKER_HOST_IP=host.docker.internal && node .",
    "start-prod": "export NODE_ENV=production && export DOCKER_HOST_IP=host.docker.internal && node .",
    "start": "npm run start-dev",
    "start-OLD": "export NODE_ENV=production && npm run build && npm run serve",
    "start-dev-OLD": "export NODE_ENV=dev && npm run build && npm run serve",
    "win-start": "set NODE_ENV=production&& npm run build && npm run serve",
    "win-start-dev": "set NODE_ENV=dev&& npm run build && npm run serve",
    "dev": "ts-node ./server/server.ts",
    "build": "npm run build-ts && npm run tslint",
    "serve": "node dist/server.js",
    "watch-node": "nodemon dist/server.js",
    "watch": "concurrently -k -p \"[{name}]\" -n \"Sass,TypeScript,Node\" -c \"yellow.bold,cyan.bold,green.bold\" \"npm run watch-sass\" \"npm run watch-ts\" \"npm run watch-node\"",
    "test": "jest --forceExit --coverage --verbose",
    "watch-ts": "tsc -w",
    "copy-static-assets": "ts-node copyStaticAssets.ts",
    "debug": "set NODE_ENV=dev && npm run build && node --inspect dist/server.js",
    "serve-debug": "nodemon --inspect dist/server.js",
    "watch-debug": "concurrently -k -p \"[{name}]\" -n \"Sass,TypeScript,Node\" -c \"yellow.bold,cyan.bold,green.bold\" \"npm run watch-sass\" \"npm run watch-ts\" \"npm run serve-debug\""
  },
  "dependencies": {
    "@types/config": "0.0.36",
    "@types/express": "^4.17.6",
    "@types/node": "^12.12.32",
    "@types/request": "^2.48.1",
    "@types/request-promise-native": "^1.0.15",
    "@types/text-table": "^0.2.0",
    "body-parser": "^1.18.3",
    "config": "^3.3.1",
    "express": "^4.17.1",
    "mysql": "^2.16.0",
    "nodemon": "^1.18.9",
    "reflect-metadata": "^0.1.13",
    "request": "^2.88.2",
    "request-promise": "^4.2.2",
    "request-promise-native": "^1.0.8",
    "text-table": "^0.2.0",
    "ts-node": "^8.10.2",
    "typeorm": "^0.2.25",
    "typescript": "^3.9.5"
  },
  "devDependencies": {
    "tslint": "^6.1.2"
  }
}
